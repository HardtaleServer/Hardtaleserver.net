Hardtale DEV Next Steps (Plugin + Backend)
Date: 2026-02-17

Goal
- Stop 401 poll failures and confirm Java plugin fulfillment loop is healthy in DEV.

Do this in order
1) Confirm backend token env on Render
- Set one shared token value in Render env:
  - HARDTALE_API_TOKEN=fed668d24106dcd1b9cee1b2ddfb919f1eb72d5d1637e7a6d76f21793614f173fbc22bcdd5af7436402b1a80ba8cfd08
- Optional fallback vars (same value if used):
  - FULFILLMENT_API_TOKEN=fed668d24106dcd1b9cee1b2ddfb919f1eb72d5d1637e7a6d76f21793614f173fbc22bcdd5af7436402b1a80ba8cfd08
  - LINK_SERVICE_AUTH_TOKEN=fed668d24106dcd1b9cee1b2ddfb919f1eb72d5d1637e7a6d76f21793614f173fbc22bcdd5af7436402b1a80ba8cfd08
- Redeploy after env changes.

2) Configure plugin token and routes (DEV server)
- HARDTALE_API_BASE_URL=https://hardtaleserver-net.onrender.com
- HARDTALE_API_TOKEN=<same shared-token as Render>
- HARDTALE_SERVER_NAME=dev
- HARDTALE_FULFILLMENT_PENDING_PATH=/api/fulfillment/pending
- HARDTALE_FULFILLMENT_ACK_PATH=/api/fulfillment/ack

3) Ensure plugin auth headers are sent correctly
- Authorization: Bearer <token>   (exact "Bearer " prefix, with space)
- X-Service-Auth: <token>
- Do not send Authorization when token is blank.

4) Restart plugin and check startup logs
- Must show tokenConfigured=true
- Must show resolved base URL and route paths.

5) Validate endpoint manually with same token
- curl -i "https://hardtaleserver-net.onrender.com/api/fulfillment/pending?server=dev&limit=1" -H "Authorization: Bearer TOKEN" -H "X-Service-Auth: TOKEN"

Interpret results quickly
- 200: Auth accepted; plugin should poll normally.
- 401 Missing Authorization bearer token: malformed/blank bearer header.
- 403 Invalid server secret: token mismatch plugin vs Render env.
- 503 Plugin API token is not configured: backend env token missing.

6) Confirm plugin poll + ack behavior
- Poll returns 200 at least once.
- On any action, plugin POSTs /api/fulfillment/ack with:
  - id=<action id>
  - status=APPLIED or FAILED
  - error only for FAILED
- Ack returns success=true.

7) Watch for stability (5-10 minutes)
- No repeated 401/403 loop.
- Backoff resets after first success.

If still failing
- Log one sanitized line per failure with:
  - method, url, status, short body
  - authHeaderAttached=true|false
  - tokenConfigured=true|false
- Re-check for hidden whitespace/newline in token source.
- Confirm plugin is using /api/fulfillment/* (or deliberately /api/internal/* with SERVER_SECRET alignment).

Definition of done
- Plugin starts cleanly.
- Pending poll succeeds (200).
- Ack succeeds (success=true).
- No recurring auth warnings in steady state.
